# NSPYR × Elevate Advisory Consulting — Affiliate Signup

Open `index.html` in a browser. The logo lives at `assets/nspyr-elevate-logo.svg`.

To send submissions somewhere (e.g. a GoHighLevel form webhook), set `ENDPOINT` in `index.html`.
